#include<bits/stdc++.h>
#include<cmath>
using namespace std;
#define _USE_MATH_DEFINES
long fact(int n)
{
    if(n<=0)
        return 1;
    else
        return n*fact(n-1);
}
int main()
{
    long a = fact(15);
    cout<<pi;
}
